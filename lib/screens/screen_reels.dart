import 'package:flutter/material.dart';

class ScreenReels extends StatelessWidget {
  const ScreenReels({super.key});

  @override
  Widget build(BuildContext context) {
    return const  Scaffold(
      body: Center(
        child: Text('Reels Section is not Available'),
      ),
    );
  }
}